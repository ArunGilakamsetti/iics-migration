name: Test IICS Packaging (Python)

on:
  workflow_dispatch:

jobs:
  build-package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Create build script (if not already in repo)
        run: |
          # This step is optional if you already have build_package.py in your repo.
          # If you haven't added it yet, you can create it here.
          cat > build_package.py << 'EOF'
          # Paste the entire Python script from the previous answer here
          # (or reference it from your repository)
          EOF

      - name: Build IICS package from Git
        run: |
          python build_package.py release.zip

      - name: Upload packaged ZIP
        uses: actions/upload-artifact@v4
        with:
          name: iics-release-package
          path: release.zip